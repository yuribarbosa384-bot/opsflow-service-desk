{
  "name": "opsflow-service-desk",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema administrativo full stack com React, TypeScript, API, SQLite, validação, testes e documentação de produto.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c cyan,magenta \"npm run dev -w @opsflow/api\" \"npm run dev -w @opsflow/web\"",
    "build": "npm run build -w @opsflow/web",
    "start:api": "npm run start -w @opsflow/api",
    "test": "npm run test -w @opsflow/domain && npm run test -w @opsflow/api && npm run test -w @opsflow/web",
    "test:e2e": "playwright test",
    "test:ci": "npm run test -- --run",
    "typecheck": "npm run typecheck -w @opsflow/domain && npm run typecheck -w @opsflow/api && npm run typecheck -w @opsflow/web"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^10.0.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
